<GridLayout>

    canvas:
        Color:
            rgba: 0.4, 0.6, 0, 1
        Rectangle:
            pos: self.pos
            size: self.size

<Grid>

    canvas:
        Color:
            rgba: 1, 1, 1, 1
        Rectangle:
            pos: self.pos
            size: self.size

<Block>

    canvas:
        Color:
            rgba: 1, 1, 0, 1
        Rectangle:
            pos: self.pos
            size: self.size


<TextInput>
    height: 30

<myButton>:
    text: '10'
    on_press: self.change()

<RootWidget>
    listHah:listHah
    AnchorLayout:

        anchor_x: 'center'
        anchor_y: 'center'
        Grid:
            size_hint: 1, 1

        GridLayout:

            size_hint: .97, .97
            rows:1

        AnchorLayout:
            padding: [dp(10),dp(10),dp(250),dp(440)]


            BoxLayout:
                anchor_x: 'left'
                anchor_y: 'top'

                padding: [dp(20),dp(10),dp(0),dp(0)]
                orientation: 'horizontal'
                BoxLayout:
                    size: 300, 1200
                    orientation: 'vertical'
                    Label:
                        text: "To-do list"
                        text_size: self.width-20, self.height-20

                    Label:

                        text: "Пользователя"
                        text_size: self.width-20, self.height-20
                    TextInput:

                        size_hint: (.2, None)
                        height: 30
                        multiline: False


                ScrollView:

                    do_scroll_x: False
                    do_scroll_y: True
                    size_hint: 3, None
                    GridLayout:
                        size: 500, 1200
                        id: listHah
                        cols:1
                        spacing: 20
                        size_hint_y: None
                        Label:
                            size_hint_y: None
                            height: self.texture_size[1]
                            text_size: self.width, None
                            padding: 10, 10
                            text: "To-do list"
                            text_size: self.width-20, self.height-20


                        TextInput:
                            size_hint: (3, None)
                            height: 30
                            multiline: False

                        TextInput:

                            size_hint: (1.8, None)
                            height: 30
                            multiline: False
                        TextInput:

                            size_hint: (1.8, None)
                            height: 30
                            multiline: False
        BoxLayout:
            padding: [dp(700),dp(10),dp(0),dp(0)]
            Button:
                text: "add"
                font_size: "10sp"
                size: 90, 50
                padding_x: 80
                size_hint: None, None
                on_press: root.change()
